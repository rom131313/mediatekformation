{% extends "base.html.twig" %}

{% block body %}
    <div class="container">
        <h2>Back-Office</h2>
        <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
            <ul class="navbar-nav">
                <li class="nav-item"><a class="nav-link" href="{{ path('admin_formations_index') }}">Formations</a></li>
                <li class="nav-item"><a class="nav-link" href="{{ path('admin_playlists_index') }}">Playlists</a></li>
                <li class="nav-item"><a class="nav-link" href="{{ path('admin_categories_index') }}">Catégories</a></li>
            </ul>
            <a class="btn btn-danger ml-auto" href="{{ path('app_logout') }}">Déconnexion</a>
        </nav>
        <div class="mt-4">
            {% block admin_content %}{% endblock %}
        </div>
    </div>
{% endblock %}
